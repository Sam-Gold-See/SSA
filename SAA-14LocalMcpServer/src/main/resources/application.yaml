server:
  port: 8014
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: SAA-14LocalMcpServer
  ai:
    mcp:
      server:
        version: 1.0.0
        type: async
        name: custom-define-mcp-server